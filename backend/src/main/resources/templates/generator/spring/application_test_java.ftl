package ${basePackage};

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ApplicationTests {

    @Test
    void contextLoads() {
        // Valida que el contexto Spring Boot y los repositorios JPA inicialicen correctamente
    }
}
