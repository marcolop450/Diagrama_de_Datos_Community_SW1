spring:
  datasource:
    url: jdbc:postgresql://${SUPABASE_DB_HOST:db.ttopqwgoilgqkphwptjf.supabase.co}:5432/postgres?sslmode=require
    username: ${SUPABASE_DB_USER:postgres}
    password: ${SUPABASE_DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
