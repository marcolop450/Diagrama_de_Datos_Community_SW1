spring:
  datasource:
    url: jdbc:postgresql://${SUPABASE_DB_HOST:aws-0-us-east-2.pooler.supabase.com}:${SUPABASE_DB_PORT:5432}/postgres?sslmode=require
    username: ${SUPABASE_DB_USER:postgres.ttopqwgoilgqkphwptjf}
    password: ${SUPABASE_DB_PASSWORD:Omegakakis54}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
